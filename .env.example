# Leave empty for local dev (Vite proxies /api to localhost:8787).
# In production, set this to your deployed backend's URL, e.g.:
# VITE_API_BASE_URL=https://edgefinder-api.onrender.com
VITE_API_BASE_URL=
